{
	"rabbitConfiguration": {
		"rabbitHost": "${rabbit-host}",
		"rabbitUser": "${rabbit-user}",
		"rabbitPassword": "${rabbit-password}",
		"maxAttempts": "${rabbit-maxattempts}",
		"backoffInterval": "${rabbit-backoffInterval}",
		"maxBackoffInterval": "${rabbit-maxBackoffInterval}",
		"rabbitPort": "${rabbit-port}"
	},
	"retryLimit": 10,
	"rejectedQueue": "${marathon-group}-langdetect-rejected",
	"prefetchBuffer": 1,
	"inputQueue": "${marathon-group}-langdetect-in"
}